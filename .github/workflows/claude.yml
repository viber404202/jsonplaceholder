name: Claude Code

# Thin caller — the actual logic lives in the org-wide reusable workflow at
# dialpad-dev/claude-workflows. Update that repo to change behavior everywhere.

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]
  pull_request:
    types: [opened, reopened, ready_for_review]

jobs:
  resolve:
    if: |
      github.event_name == 'pull_request' ||
      contains(github.event.comment.body, '@claude') ||
      contains(github.event.review.body, '@claude') ||
      contains(github.event.issue.body, '@claude') ||
      contains(github.event.issue.title, '@claude')
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions: {}
    outputs:
      actor_secret_name: ${{ steps.resolve.outputs.actor_secret_name }}
      pr_author_secret_name: ${{ steps.resolve.outputs.pr_author_secret_name }}
    steps:
      - name: Resolve developer token secret name
        id: resolve
        env:
          # Comment/mention-driven runs act as the commenter; automatic PR
          # reviews act as the PR author.
          ACTOR: ${{ github.actor }}
          PR_AUTHOR: ${{ github.event.pull_request.user.login || github.actor }}
        run: |
          # Secret names allow only letters, digits and `_`, so uppercase the
          # username and turn every `-` into `_` (jane-doe -> JANE_DOE).
          secret_name() {
            printf 'CLAUDE_TOKEN_%s' "$(printf '%s' "$1" | tr '[:lower:]-' '[:upper:]_')"
          }
          echo "actor_secret_name=$(secret_name "$ACTOR")" >> "$GITHUB_OUTPUT"
          echo "pr_author_secret_name=$(secret_name "$PR_AUTHOR")" >> "$GITHUB_OUTPUT"

  claude:
    needs: resolve
    uses: viber404202/claude-workflows/.github/workflows/claude.yml@main
    permissions:
      contents: read
      pull-requests: write
      issues: read
      id-token: write
      actions: read
    secrets:
      actor_token: ${{ secrets[needs.resolve.outputs.actor_secret_name] }}
      pr_author_token: ${{ secrets[needs.resolve.outputs.pr_author_secret_name] }}
